# Nix build artifacts and temporary files
result
.direnv
.envrc

# Backup files
*.backup
*.bak
*.tmp

# Editor and IDE
*~
.*.swp
.*.swo
.vscode/
.idea/
.cursor/
__pycache__/

# OS files
.DS_Store
Thumbs.db

# Local agent planning scratch (not ~/.hermes runtime config)
.hermes/

# code-review-graph MCP local database (absolute paths, machine-specific)
.code-review-graph/

# AI stack secrets — never commit API keys
ai-stack/.env

# AI stack runtime output (rendered MCP JSON/YAML, stale Claude/vLLM artifacts)
ai-stack/generated/

# Legacy compose env from removed vLLM stack (safe to delete locally)
ai-stack/models.compose.env
